general {
    col.active_border = rgba(8b9296AA)
    col.inactive_border = rgba(41484cAA)
}

misc {
    background_color = rgba(111415FF)
}

plugin {
    hyprbars {
        # Honestly idk if it works like css, but well, why not
        bar_text_font = Rubik, Geist, AR One Sans, Reddit Sans, Inter, Roboto, Ubuntu, Noto Sans, sans-serif
        bar_height = 30
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = true
        bar_part_of_window = true

        bar_color = rgba(111415FF)
        col.text = rgba(e2e2e4FF)


        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        hyprbars-button = rgb(e2e2e4), 13, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgb(e2e2e4), 13, 󰖯, hyprctl dispatch fullscreen 1
        hyprbars-button = rgb(e2e2e4), 13, 󰖰, hyprctl dispatch movetoworkspacesilent special
    }
}

windowrulev2 = bordercolor rgba(a0cde6AA) rgba(a0cde677),pinned:1
